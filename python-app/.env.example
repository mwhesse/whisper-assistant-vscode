# Server Configuration
HOST=0.0.0.0
PORT=4445

# Whisper Model Configuration
WHISPER_MODEL=base
WHISPER_DEVICE=cpu
WHISPER_COMPUTE_TYPE=int8

# CORS Configuration
CORS_ORIGINS=*

# Default Settings
DEFAULT_LANGUAGE=en
TEMP_FILE_SUFFIX=.wav

# API Configuration
API_TITLE=WhisperX Assistant API
API_VERSION=1.0.0

# Model Storage Configuration
# Enable external model storage (default: false)
ENABLE_EXTERNAL_STORAGE=false

# Custom cache directory for models (optional)
# If not set, will use HF_HOME or default HuggingFace cache
# MODELS_CACHE_DIR=/path/to/external/models

# Volume mount path inside container (default: /app/models)
MODELS_VOLUME_PATH=/app/models

# HuggingFace cache directory (optional, overrides default ~/.cache/huggingface)
# HF_HOME=/path/to/hf/cache

# Transformers cache directory (optional)
# TRANSFORMERS_CACHE=/path/to/transformers/cache

# Logging (optional)
# LOG_LEVEL=INFO